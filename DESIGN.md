# KnowForge 指标口径与数据字典：独立项目设计

状态：已按用户确认的要求实施，验证状态见 README.md。目录：`C:/Projects/knowforge-metrics-rag`。

## 1. 项目边界

从原项目提取企业指标口径与数据字典场景，形成可独立安装、配置、入库和启动的项目。原项目代码与运行数据保持独立。新项目不展示其他业务场景，不通过父目录 import 原项目。

保留 FastAPI、LangChain、Milvus、MySQL 和本地 BGE 模型技术栈，Redis 按配置启用。迁入必要的 RAG、表格处理、来源引用和版本管理代码；不复制旧场景、课程站点、历史日志或真实密钥。本地模型用配置指定路径，避免重复复制大文件。

新项目使用独立数据库配置、Milvus 集合名称和运行端口（建议 8001）。不自动复用原项目的会话、账号或激活指针。原指标演示资料需在新项目中重新入库。

## 2. 页面与导航

以 KnowForge 为品牌，采用深绿色、暖白背景与清晰的内容卡片，重新组织信息布局。

- 登录页：品牌介绍、账号密码、错误提示和登录状态；登录成功进入工作台。
- 工作台：当前知识库概况、四个业务分类入口、最近会话和示例问题。
- 知识分类模块：独立页面展示指标口径、数据字典、版本与变更、数据血缘，提供分类说明、所属资料和进入问答入口。
- 智能问答：会话列表、问答正文、来源引用面板；可选择已授权分类，支持全库问答。
- 账号信息：展示所属组织、角色和已授权数据集；普通用户不能自行修改权限。
- 管理区：管理员维护用户授权、查看知识库版本与质量报告。非管理员不能访问管理接口。

分类模块首先复用已存在的四类 source，不引入另一套重叠的分类体系。文档浏览仅展示账号有权访问的资料。

## 3. 登录与账号权限

第一版采用本地账号密码登录，由管理员创建账号，不开放自行注册。不预置公开通用密码，首次管理员通过本地初始化命令建立。

账号持久化保存所属租户、角色、可访问数据集和可见范围。密码使用带盐的密码哈希；会话使用服务端可撤销的登录会话和 HttpOnly Cookie，并实现过期、退出、登录限流及必要的请求来源/CSRF检查。

角色建议：

| 角色 | 权限 |
| --- | --- |
| 普通用户 | 在授权范围内问答、查看自己的历史和来源 |
| 知识管理员 | 维护授权范围内知识库版本及资料质量 |
| 租户管理员 | 管理本租户账号及授权，不能跨租户访问 |

用户可选数据集必须来自其已授权列表。前端不再提供可随意填写的 tenant、role、visibility 输入框。后端根据登录身份解析数据域，不能信任客户端提交的角色或租户。

权限校验覆盖 HTTP、WebSocket、来源与文档预览、会话历史、反馈、检索调试及版本管理。会话绑定用户和租户；猜测 session_id 不得读取其他用户历史。拒绝越权请求应发生在检索之前，缓存键同时包含有效的数据权限范围。

新项目需增加账号、授权和登录会话等数据库表，并提供明确的初始化脚本；只作用于新项目数据库。

## 4. RAG 行为

保留指标与字段的演示资料和回答边界：解释定义、公式、粒度、分子分母、单位、生效区间和数据来源。仅精确 FAQ 可直接回答；相似问法进入检索生成流程。业务版本与知识库版本分别处理。

不宣称具备实时指标查询、业务 SQL 执行或生产血缘自动扫描能力。资料不足或历史版本缺失时明确提示。账号登录与数据隔离必须为真实后端功能，不能仅在 UI 上隐藏控件。

## 5. 实现与验收顺序

1. 提取最小运行代码与当前业务资料，建立独立配置、启动和入库入口。
2. 实现账号、登录会话与授权模型，接通服务端数据域校验。
3. 建立新 UI、登录页、工作台和独立分类模块。
4. 接通问答、历史、引用和管理员功能。
5. 运行登录失败/退出、会话归属、跨租户越权、伪造角色、WebSocket鉴权等测试。
6. 验证新项目入库、检索与端到端页面流程，整理 README 和实际测试记录。

验收标准：新目录无需原项目源码即可启动；未登录不可访问业务数据；修改客户端参数不能提升权限；四类业务可独立浏览和进入问答；旧项目保持可用。真实服务依赖无法连接时，应明确报告未验证部分。
